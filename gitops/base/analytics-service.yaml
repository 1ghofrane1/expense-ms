apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  labels:
    app: analytics-service
spec:
  selector:
    app: analytics-service
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
      name: http
